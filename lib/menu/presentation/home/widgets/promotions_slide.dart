import 'package:flutter/material.dart';

class PromotionsSlide extends StatelessWidget {
  const PromotionsSlide({super.key});

  @override
  Widget build(BuildContext context) {
    return Card(
      child: SizedBox(
        width: double.infinity,
        child: Row(children: [Text("aaaaa")]),
      ),
    );
  }
}
